# Ontop endpoint configuration — connects to Neo4j via the official Neo4j JDBC driver v6.x.
#
# Hostname `neo4j` resolves inside the docker-compose network to the Neo4j service.
# The `?sql2cypher=true` URL param enables the driver's SQL→Cypher translator
# (it is OFF by default in v6.x).

jdbc.url=jdbc:neo4j://neo4j:7687?sql2cypher=true
jdbc.user=neo4j
jdbc.password=Password123
jdbc.driver=org.neo4j.jdbc.Neo4jDriver
